import React, { useState } from 'react';\nimport { X, Save } from 'lucide-react';\nimport { Input } from '@/components/ui/input';\nimport { Button } from '@/components/ui/button';\n\ninterface MobileStockEditProps {\n  productName: string;\n  currentStock: number;\n  onSave: (newStock: number) => void;\n  onClose: () => void;\n}\n\nconst MobileStockEdit: React.FC<MobileStockEditProps> = ({\n  productName,\n  currentStock,\n  onSave,\n  onClose,\n}) => {\n  const [stock, setStock] = useState(currentStock.toString());\n\n  const handleSave = () => {\n    const newStock = parseInt(stock, 10);\n    if (!isNaN(newStock) && newStock >= 0) {\n      onSave(newStock);\n      onClose();\n    }\n  };\n\n  return (\n    <div className=\"fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4\">\n      <div className=\"bg-admin-card border border-admin-border rounded-lg w-full max-w-sm\">\n        {/* Header */}\n        <div className=\"flex items-center justify-between p-4 border-b border-admin-border\">\n          <h2 className=\"text-lg font-semibold text-admin-text-primary truncate\">\n            {productName}\n          </h2>\n          <button\n            onClick={onClose}\n            className=\"p-1 hover:bg-[#C4A97D]/10 rounded transition-colors\"\n          >\n            <X className=\"w-5 h-5\" />\n          </button>\n        </div>\n\n        {/* Content */}\n        <div className=\"p-4 space-y-4\">\n          <div>\n            <label className=\"text-xs text-admin-text-secondary mb-2 block\">\n              Stock actuel\n            </label>\n            <p className=\"text-2xl font-bold text-admin-gold mb-4\">\n              {currentStock} unités\n            </p>\n          </div>\n\n          <div>\n            <label className=\"text-xs text-admin-text-secondary mb-2 block\">\n              Nouveau stock\n            </label>\n            <Input\n              type=\"number\"\n              value={stock}\n              onChange={(e) => setStock(e.target.value)}\n              className=\"w-full bg-admin-bg border-admin-border\"\n              placeholder=\"Entrez la nouvelle quantité\"\n            />\n          </div>\n\n          {parseInt(stock, 10) !== currentStock && (\n            <div className=\"bg-admin-gold/10 border border-admin-gold/30 rounded p-3\">\n              <p className=\"text-xs text-admin-gold\">\n                Changement: {parseInt(stock, 10) - currentStock > 0 ? '+' : ''}\n                {parseInt(stock, 10) - currentStock}\n              </p>\n            </div>\n          )}\n        </div>\n\n        {/* Actions */}\n        <div className=\"flex gap-2 p-4 border-t border-admin-border\">\n          <Button\n            variant=\"outline\"\n            onClick={onClose}\n            className=\"flex-1\"\n          >\n            Annuler\n          </Button>\n          <Button\n            onClick={handleSave}\n            className=\"flex-1 bg-admin-gold hover:bg-admin-gold-light text-admin-bg\"\n          >\n            <Save className=\"w-4 h-4 mr-2\" />\n            Enregistrer\n          </Button>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default MobileStockEdit;
